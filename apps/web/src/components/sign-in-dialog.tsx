'use client'

// This is an empty component that replaces the old sign-in functionality
// It can be safely removed once all imports are cleaned up
export default function SignInDialog() {
    return null
} 